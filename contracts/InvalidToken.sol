pragma solidity 0.5.1;

/**
 * @title Invalid token implementation
 * @author Maarten Zuidhoorn
 * @dev This contract exists purely for testing purposes
 */
contract InvalidToken {
  /**
   * @dev This function does nothing, it exists for testing purposes only
   */
  function foo() public pure {
    // empty
  }
}
