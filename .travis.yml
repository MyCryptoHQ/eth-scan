language: node_js

cache:
  yarn: true

install:
  - yarn --silent

script:
  - yarn run compile
  - yarn run test
  - yarn run lint

before_deploy:
  - yarn run build

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_API_KEY
  skip_cleanup: true
  on:
    branch: master
    tags: true
